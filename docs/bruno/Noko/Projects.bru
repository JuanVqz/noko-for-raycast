meta {
  name: Projects
  type: http
  seq: 2
}

get {
  url: {{domain}}/projects?enabled=true&per_page=100
  body: none
  auth: inherit
}

params:query {
  enabled: true
  per_page: 100
}

headers {
  X-NokoToken: {{x_noko_token}}
}

settings {
  encodeUrl: true
}
